name: hello workflow
on: push
jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo '${{ toJSON(github) }}'
      - name: Print Actor
        run: echo "${{ github.actor }}"
