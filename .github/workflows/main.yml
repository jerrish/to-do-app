name: hello workflow
on: push
jobs:
  say-hello1:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "hello1, github actions!"
  say-hello2:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "hello2, github actions!"
